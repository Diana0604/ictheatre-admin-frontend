import { IConfig } from "../types/types.config";

const config: IConfig = {
  //baseUrl: `http://localhost:3000`, //baseurl where to ask to API
  baseUrl: `http://192.168.100.1:3000`, //baseurl where to ask to API
};

export default config;
