import Navbar from '../components/navbar/NavBar'

/**
 * Home page with links to other pages -> might change it to something else
 * @returns
 */
export default function Home() {

  return (
    <Navbar />
  )
}
